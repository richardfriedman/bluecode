
#############################################################################################
# This file is automatically generated by the Blue framework. Please be careful when editing#
# this file by hand as any mistakes may cause unexpected behaviour in your monitoring setup.#
#############################################################################################

  
define command{
	command_name		check_ping
	command_line		java -jar $USER2$/blue-check-ping.jar -H $HOSTADDRESS$ -w $ARG1$ -c $ARG2$ -p 5
}  	 

define command{
	command_name		check_local_load
	command_line		$USER1$/check_load -w $ARG1$ -c $ARG2$
}  	 
  
define command{
	command_name		check-host-alive
	command_line		java -jar $USER2$/blue-check-ping.jar -H $HOSTADDRESS$ -w 3000.0,80% -c 5000.0,100% -p 1
}  	 

define command{
	command_name		check_local_disk
	command_line		$USER1$/check_disk -w $ARG1$ -c $ARG2$ -p $ARG3$
}

define command{
	command_name		check_local_time
	command_line		java -jar $USER2$/blue-check-local-time.jar -H $ARG1$ -w $ARG2$ -c $ARG3$
}

define command{
	command_name		check_ftp
	command_line		java -jar $USER2$/blue-check-ftp.jar -H $ARG1$ -d $ARG2$ -f $ARG3$ -u $ARG4$ -p $ARG5$	 
}
 
# For information on converting this to actually email your contacts, please see
# the send_mail plugin within your Blue directory. You can use this to send emails
# via Blue.
   
define command{
	command_name		email_contact
	command_line		echo "Emailing your Blue Contact!"
}  	 
  	 
# The Global Event handler here is an example only. Its functionality is in essence worthless,
# but is useful in demonstrating the concepts of Blue. 
  
define command{
	command_name		global_event_handler
	command_line		echo "A Global Event Has Occurred"
}  	 
 	 

#EOF
